projects:
  app:
    schemaPath: 'src/schema.graphql'
    extensions:
      endpoints:
        default: 'http://localhost:4000'
  prisma:
    schemaPath: 'src/generated/prisma-client/prisma-schema.ts'
    extensions:
      prisma: prisma/prisma.yml
